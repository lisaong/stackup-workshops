# Note: set credentials in the Lambda dashboard or pass them through -e.
# (These are intentionally blank)
functions:
  aws:
  - lambda:
      name: scar-sgcarmart
      container:
        image: lisaong/monitor-carmart:1.0
        environment:
          Variables:
            CARMART_QUERIES: mx-5;brz;toyota+86;mini+cooper
            S3_BUCKET_NAME: carmart
            AWS_ACCESS_KEY_ID: ''
            AWS_SECRET_ACCESS_KEY: ''